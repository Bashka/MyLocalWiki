= Описание =
`docker logs [OPTIONS] CONTAINER`

Вывод логов (STDOUT) контейнера.

= Аргументы =
* `-f` - интерактивный режим (чтение логов не прекращается)
* `--tail=5` - количество последних записей лога, включаемых в вывод

= Пример =
{{{bash
docker logs -f my_container
}}}
