= Описание =
`docker kill [OPTIONS] CONTAINER`

Принудительно останавливает контейнер.

= Аргументы =
* `--signal=9` - передает заданный сигнал всем процессам в контейнере

= Пример =
{{{bash
docker kill my_container
}}}
